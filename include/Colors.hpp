#pragma once

#include <SFML/Graphics/Color.hpp>

namespace Colors {
    const sf::Color kWolfColor(sf::Color::Red);
    const sf::Color kHumanColor(sf::Color::Blue);
    const sf::Color kPlantColor(sf::Color::Green);
    const sf::Color kWallColor(sf::Color(128, 128, 128));
    const sf::Color kGridColor(sf::Color::Black);
    const sf::Color kBackgroundColor(sf::Color::White);
}
